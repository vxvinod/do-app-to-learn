<%= form_for [@traversable, Traversal.new] do |f| %>
	<% if @traversal.errors.any? %>
		<% @traversal.errors.full_messages.each do |traversal| %>
		<%= traversal%>
		<%end%>
	<% end %>
	<%= f.text_field :content %>
	<%= f.submit "submit traversal"%>
<%end%>